import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  mainContainer: {
    borderBottomColor: '#003153',
    borderBottomWidth: 1,
    display: 'flex',
    marginBottom: 10
  },
  label: {
    color: '#003153'
  }
});

export default styles;